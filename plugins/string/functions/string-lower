emulate -L zsh; setopt local_options extended_glob
local str newstr exitcode=1
for str in "$@"; do
  newstr="${(L)str}"
  [[ "$str" != "$newstr" ]] && exitcode=0
  echo $newstr
done
return $exitcode

# vim: ft=zsh
