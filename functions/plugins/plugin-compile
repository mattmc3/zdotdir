local repo plugin_name plugin_dir f
ZPLUGINDIR=${ZPLUGINDIR:-${ZDOTDIR:-$HOME/.config/zsh}/plugins}
autoload -U zrecompile
for repo in $@; do
  plugin_name=${repo:t}
  plugin_dir=$ZPLUGINDIR/$plugin_name
  for f in $plugin_dir/**/*.zsh{,-theme}(N); do
    zrecompile -pq "$f"
  done
done
