function del {
  if type rem &>/dev/null; then
    touch -- "$@"
    rem -- "$@"
  elif type shell-safe-rm &>/dev/null; then
    shell-safe-rm -- "$@"
  else
    echo "shell-safe-rm not found: https://github.com/kaelzhang/shell-safe-rm" >&2
    echo "/bin/rm $@"
  fi
}
